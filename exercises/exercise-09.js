// Use the functions defined below to complete the exercise.  

function add(a, b) {
    console.log(a + b);
}

function sub(a, b) {
    console.log(a - b);
}

var a = 5;
var b = 3;

// Call the add function and pass in the variables a & b defined above.
// Should display 8

// Call the sub function and pass in the variables a & b defined above.
// Should display 2

a = 6;
b = 2;

// Call the add function and pass in the variables a & b defined above.
// Should display 8

// Call the sub function and pass in the variables a & b defined above.
// Should display 4

var x = 4;
var y = 2;

// Call the add function and pass in the variables x & y defined above.
// Should display 6

// Call the sub function and pass in the variables x & y defined above.
// Should display 2
